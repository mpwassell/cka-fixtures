
$( document ).ready(function() {

	$("a").button();

/*
	$("#fixture_btn" ).button().click(function( event ) {
		window.location="fixtures";
	});

	$.each( [1,2,3],function(i,v) {
		$("#division" + v + "_btn" ).button().click(function( event ) {
			window.location="results/" + v;
		});
		$("#division" + v + "_goals_btn" ).button().click(function( event ) {
			window.location="goals/" + v;
		});
	});

	l = [ [ 'results_xml', 'results_xml'],
		  [ 'team_stas', 'file:///E:/Mark/CKAWebSite/SeasonFrame.htm' ],
		  [ 'player_status'], 'status' ]

	$("#results_xml_btn" ).button().click(function( event ) {
		window.location="results_xml";
	});
	
	$("#team_stats_btn" ).button().click(function( event ) {
		window.location="file:///E:/Mark/CKAWebSite/SeasonFrame.htm";
	});

	$("#player_status_btn" ).button().click(function( event ) {
		window.location="status";
	});
	
	$("#serl_goals_btn" ).button().click(function( event ) {
		window.location="goals/-1";
	});
*/


	
			
});

