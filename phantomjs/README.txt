Beginnings of code to automate upload of results to fixtures live.

We use Phantom JS, a head-less WebKit scriptable with JS, rather than Selenium. The latter in my
experience is slow and the browser itself is not required for what we need to do.

Also use CasperJS

1. Basic results entry - Enter match scores for matches that have been played.

2. Match details entry.